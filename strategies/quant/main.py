import pandas as pd
import numpy as np
from config import ACTIVE_CONFIG
from strategies import STRATEGY_MAP
from interface import input_adapter, output_adapter


def generate_signals(raw_data: pd.DataFrame) -> pd.DataFrame:
    # Fetch Strategy and Configs

    # Adapt input data

    # Alpha Feature assembly

    # Alpha score generation

    # Context Feature assembly

    # Signal generation

    # Adapt output signals

    pass